---
phase: 03-accounts-cloud-sync
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/features/auth/screens/login_screen.dart
  - lib/features/auth/screens/signup_screen.dart
  - lib/features/auth/screens/forgot_password_screen.dart
  - lib/features/auth/viewmodels/auth_viewmodel.dart
  - lib/features/home/screens/home_screen.dart
  - lib/features/home/viewmodels/home_viewmodel.dart
  - lib/features/home/widgets/streak_ring.dart
  - lib/features/home/widgets/goal_ring.dart
  - lib/features/home/widgets/scenario_cards.dart
  - lib/features/home/widgets/guest_banner.dart
  - lib/main.dart
autonomous: true
requirements: [PLAT-02]

must_haves:
  truths:
    - LoginScreen displays email/password fields, Google sign-in button, "Continue as Guest" link, and "Forgot Password" link
    - SignUpScreen displays name/email/password fields, Google sign-up button, and "Already have an account" link
    - ForgotPasswordScreen displays email field and sends reset link
    - HomeScreen shows welcome header, daily goal ring, streak indicator, recommended scenario cards, and guest banner when not authenticated
    - Navigation flow is: SplashScreen → Onboarding → Home → ScenarioSelection → Conversation → Feedback
    - Unauthenticated non-guest users are redirected to LoginScreen
  artifacts:
    - lib/features/auth/screens/login_screen.dart
    - lib/features/auth/screens/signup_screen.dart
    - lib/features/auth/screens/forgot_password_screen.dart
    - lib/features/auth/viewmodels/auth_viewmodel.dart
    - lib/features/home/screens/home_screen.dart
    - lib/features/home/viewmodels/home_viewmodel.dart
    - lib/features/home/widgets/streak_ring.dart
    - lib/features/home/widgets/goal_ring.dart
    - lib/features/home/widgets/scenario_cards.dart
    - lib/features/home/widgets/guest_banner.dart
  key_links:
    - AuthViewModel calls AuthService methods and catches errors for UI display
    - HomeViewModel loads user data from FirestoreService (authenticated) or SharedPreferences (guest)
    - Route table in main.dart adds /login, /signup, /forgot-password, /home routes
    - SplashScreen checks auth state: authenticated → /home, guest with onboarding done → /home, new user → /onboarding
---

<objective>
Build the authentication screens (login, sign-up, forgot password) and home dashboard with navigation integration.

Purpose: Delivers the user-facing auth UI and the new home dashboard that serves as the post-onboarding landing screen. Connects all routes so the new navigation flow works end-to-end.

Output: Auth screens with ViewModels, Home screen with widgets, updated route table in main.dart.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-accounts-cloud-sync/03-RESEARCH.md
@.planning/phases/03-accounts-cloud-sync/03-01-SUMMARY.md
@lib/main.dart
@lib/features/splash/splash_screen.dart
@lib/features/scenario_selection/screens/scenario_selection_screen.dart
@lib/features/onboarding/screens/onboarding_screen.dart
@lib/core/providers/auth_provider.dart
@lib/core/services/auth_service.dart
@lib/core/services/firestore_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthViewModel, LoginScreen, SignUpScreen, and ForgotPasswordScreen</name>
  <files>
    lib/features/auth/viewmodels/auth_viewmodel.dart
    lib/features/auth/screens/login_screen.dart
    lib/features/auth/screens/signup_screen.dart
    lib/features/auth/screens/forgot_password_screen.dart
  </files>
  <read_first>
    - lib/features/onboarding/viewmodels/onboarding_viewmodel.dart (see Notifier pattern, state class, provider declaration)
    - lib/features/feedback/screens/feedback_screen.dart (see screen pattern: pure UI, watches provider, no business logic)
    - lib/core/providers/auth_provider.dart (created in Plan 1 — use authServiceProvider, currentUserProvider)
    - lib/core/services/auth_service.dart (created in Plan 1 — see method signatures)
    - lib/core/theme/app_theme.dart (see theme: pastel colors, claymorphism style)
  </read_first>
  <action>
Create `lib/features/auth/viewmodels/auth_viewmodel.dart`:

AuthState class with fields:
- `bool isLoading` (default false)
- `String? errorMessage`
- `bool passwordResetSent` (default false)

AuthViewModel extends Notifier<AuthState>:
- `build()` returns initial AuthState
- `Future<void> signInWithEmail(String email, String password)` — sets isLoading, calls authService.signInWithEmail, clears error on success, sets errorMessage on failure
- `Future<void> signUpWithEmail(String email, String password, String displayName)` — same pattern
- `Future<void> signInWithGoogle()` — same pattern
- `Future<void> signInAsGuest()` — calls authService.signInAnonymously, same error pattern
- `Future<void> sendPasswordReset(String email)` — calls authService.sendPasswordReset, sets passwordResetSent on success
- `void clearError()` — resets errorMessage to null

Import authServiceProvider from core/providers/auth_provider.dart. Use `ref.read(authServiceProvider)` to get AuthService. Never import widgets.

Create auth screens (follow feedback_screen.dart pattern: StatelessWidget, watches provider, pure UI):

`lib/features/auth/screens/login_screen.dart` — LoginScreen:
- Email field (TextFormField with email keyboard type, validator: non-empty, contains @)
- Password field (TextFormField with obscureText, validator: non-empty, min 6 chars)
- "Sign In" ElevatedButton (full width, pastel theme) — calls authViewModel.signInWithEmail
- "Sign in with Google" button (with Google icon placeholder) — calls authViewModel.signInWithGoogle
- "Continue as Guest" TextButton — calls authViewModel.signInAsGuest then navigates to /home
- "Forgot Password?" TextButton — navigates to /forgot-password
- "Don't have an account? Sign Up" TextButton — navigates to /signup
- Show CircularProgressIndicator when isLoading
- Show error SnackBar when errorMessage is set
- Form key for validation

`lib/features/auth/screens/signup_screen.dart` — SignUpScreen:
- Display name field (TextFormField, validator: non-empty)
- Email field (same as login)
- Password field (same as login)
- "Sign Up" ElevatedButton — calls authViewModel.signUpWithEmail
- "Sign up with Google" button — calls authViewModel.signUpWithGoogle
- "Already have an account? Sign In" TextButton — navigates to /login
- Same loading/error patterns as LoginScreen

`lib/features/auth/screens/forgot_password_screen.dart` — ForgotPasswordScreen:
- Email field (TextFormField with email keyboard)
- "Send Reset Link" ElevatedButton — calls authViewModel.sendPasswordReset
- Show success message "Reset link sent to your email" when passwordResetSent is true
- "Back to Sign In" TextButton — navigates to /login

All screens: Scaffold with AppBar, centered form content, claymorphism-themed styling matching existing screens (use AppTheme.light colors). No business logic in screens — only watch providers and forward actions to ViewModel.
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze lib/features/auth/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/auth/viewmodels/auth_viewmodel.dart exists with AuthState (isLoading, errorMessage, passwordResetSent) and AuthViewModel (signInWithEmail, signUpWithEmail, signInWithGoogle, signInAsGuest, sendPasswordReset, clearError)
    - lib/features/auth/screens/login_screen.dart exists with email/password fields, Sign In button, Google button, Guest link, Forgot Password link, Sign Up link
    - lib/features/auth/screens/signup_screen.dart exists with name/email/password fields, Sign Up button, Google button, Sign In link
    - lib/features/auth/screens/forgot_password_screen.dart exists with email field, Send Reset Link button, success message, Back to Sign In link
    - AuthViewModel imports only from core/providers and core/services (no widget imports)
    - All screens are StatelessWidgets that watch providers (no business logic in screens)
    - flutter analyze reports 0 errors on all auth files
  </acceptance_criteria>
  <done>Auth screens (login, sign-up, forgot password) and AuthViewModel exist with full form validation, loading states, error display, and navigation links between screens.</done>
</task>

<task type="auto">
  <name>Task 2: Create HomeScreen, HomeViewModel, and dashboard widgets</name>
  <files>
    lib/features/home/screens/home_screen.dart
    lib/features/home/viewmodels/home_viewmodel.dart
    lib/features/home/widgets/streak_ring.dart
    lib/features/home/widgets/goal_ring.dart
    lib/features/home/widgets/scenario_cards.dart
    lib/features/home/widgets/guest_banner.dart
  </files>
  <read_first>
    - lib/features/scenario_selection/screens/scenario_selection_screen.dart (see screen layout pattern)
    - lib/features/scenario_selection/widgets/scenario_card.dart (see widget pattern for scenario cards)
    - lib/features/onboarding/viewmodels/onboarding_viewmodel.dart (see Notifier pattern, loadSavedData)
    - lib/core/providers/auth_provider.dart (use currentUserProvider, isGuestProvider, firestoreServiceProvider)
    - lib/core/services/firestore_service.dart (see method signatures for getProgress, getPreferences, getScenarios)
    - lib/features/scenario_selection/models/scenario.dart (see Scenario model)
  </read_first>
  <action>
Create `lib/features/home/viewmodels/home_viewmodel.dart`:

HomeState class with fields:
- `int totalXp` (default 0)
- `int scenariosCompleted` (default 0)
- `int streakDays` (default 0)
- `String? displayName`
- `String? cefrLevel`
- `List<Scenario> recommendedScenarios` (default empty)
- `bool isLoading` (default true)
- `String? error`

HomeViewModel extends AsyncNotifier<HomeState>:
- `build()` — loads data based on auth state:
  - If authenticated (not guest): read user profile from FirestoreService.getUserProfile(uid) for displayName, getProgress(uid) for xp/scenarios, getPreferences(uid) for cefrLevel, getScenarios(uid) for history. Filter scenarios by cefrLevel for recommendations.
  - If guest: read from SharedPreferences (same keys as OnboardingViewModel uses). Set displayName to "Guest". Load bundled scenarios from assets and filter by saved CEFR level.
  - Always load 3-4 recommended scenarios (from bundled JSON, filtered by user's CEFR level)
- `void refresh()` — re-runs build() to reload data (for pull-to-refresh or after completing a scenario)

Import Scenario from scenario_selection models. Import auth providers and services.

Create widgets (each is a small, focused StatelessWidget):

`lib/features/home/widgets/streak_ring.dart` — StreakRing:
- Circular indicator with flame emoji + day count
- Takes `int streakDays` parameter
- When streakDays == 0: show "Start your streak!" with grey ring
- When streakDays > 0: show flame icon and "X day streak" with accent color ring
- Styled with claymorphism: soft rounded container, pastel background

`lib/features/home/widgets/goal_ring.dart` — GoalRing:
- Circular progress indicator showing daily XP progress
- Takes `int currentXp`, `int targetXp` parameters (targetXp default 50)
- Show "Daily Goal" label, circular progress arc, "Xp / Yp" text in center
- Pastel color scheme matching theme

`lib/features/home/widgets/scenario_cards.dart` — ScenarioCards:
- Horizontal scrolling list of 3-4 recommended scenario cards
- Takes `List<Scenario> scenarios` parameter
- Each card: scenario name, CEFR level badge, persona name, tap handler
- Tap navigates to /conversation with the scenario (use selectedScenarioProvider from scenario_selection)
- Styled similar to ScenarioCard widget from scenario_selection

`lib/features/home/widgets/guest_banner.dart` — GuestBanner:
- Shown only when isGuest is true
- Message: "Sign up to save your progress!"
- "Sign Up" button → navigates to /signup
- "Maybe Later" dismiss button (sets local state to hide)
- Styled as a soft banner at top of home screen, pastel accent color

Create `lib/features/home/screens/home_screen.dart` — HomeScreen:
- Scaffold with AppBar showing "Linguo Wizard" title
- Body: SingleChildScrollView containing:
  1. Welcome header: "Hello, {displayName}!" (or "Hello, Guest!")
  2. StreakRing widget
  3. GoalRing widget
  4. GuestBanner (conditional on isGuest)
  5. ScenarioCards (recommended scenarios)
- Watch homeViewModel for data
- Loading state: show CircularProgressIndicator
- "Browse All Scenarios" TextButton → navigates to /scenarios
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze lib/features/home/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/home/viewmodels/home_viewmodel.dart exists with HomeState (totalXp, scenariosCompleted, streakDays, displayName, cefrLevel, recommendedScenarios, isLoading, error) and HomeViewModel (build, refresh)
    - lib/features/home/screens/home_screen.dart exists with welcome header, streak ring, goal ring, scenario cards, guest banner (conditional)
    - lib/features/home/widgets/streak_ring.dart exists showing flame + streak count or "Start your streak!"
    - lib/features/home/widgets/goal_ring.dart exists showing circular XP progress
    - lib/features/home/widgets/scenario_cards.dart exists with horizontal scrolling scenario cards
    - lib/features/home/widgets/guest_banner.dart exists with "Sign up to save your progress!" message
    - HomeViewModel loads data from FirestoreService when authenticated, SharedPreferences when guest
    - flutter analyze reports 0 errors on all home files
  </acceptance_criteria>
  <done>HomeScreen with dashboard widgets (streak ring, goal ring, scenario cards, guest banner) exists, HomeViewModel loads data from Firestore or SharedPreferences based on auth state, all compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Update main.dart route table and SplashScreen navigation logic</name>
  <files>lib/main.dart, lib/features/splash/splash_screen.dart</files>
  <read_first>
    - lib/main.dart (see current route table and SplashScreen wiring)
    - lib/features/splash/splash_screen.dart (see current splash behavior)
    - lib/core/providers/auth_provider.dart (use authStateProvider, isGuestProvider)
    - lib/features/onboarding/viewmodels/onboarding_viewmodel.dart (see hasCompletedOnboarding)
  </read_first>
  <action>
Update `lib/main.dart`:
- Import the new screens: LoginScreen, SignUpScreen, ForgotPasswordScreen, HomeScreen
- Add routes to the route table:
  - `/login` → LoginScreen
  - `/signup` → SignUpScreen
  - `/forgot-password` → ForgotPasswordScreen
  - `/home` → HomeScreen
- Change `initialRoute` logic:
  - Remove the old `onboardingDone ? '/scenarios' : '/onboarding'` pattern
  - SplashScreen now handles all routing decisions (it already has the `onSplashDone` callback)
  - The SplashScreen should determine the target based on auth state and onboarding completion
- Update the SplashScreen constructor: remove `onSplashDone` parameter (or keep it but SplashScreen internally decides where to navigate). The SplashScreen should:
  1. Check `FirebaseAuth.instance.currentUser`
  2. If user is null (not authenticated, not guest): navigate to `/login` (new users must auth or choose guest)
  3. If user is anonymous (guest) and onboarding is done: navigate to `/home`
  4. If user is anonymous (guest) and onboarding is NOT done: navigate to `/onboarding`
  5. If user is authenticated and onboarding is done: navigate to `/home`
  6. If user is authenticated and onboarding is NOT done: navigate to `/onboarding` (edge case: signed up on new device)
- Add `/scenarios` route to the route table (it already exists, keep it)
- Add a route guard concept: after `/home`, user can navigate to `/scenarios` → `/conversation` → `/feedback`

Update `lib/features/splash/splash_screen.dart`:
- Change from taking an `onSplashDone` callback to handling navigation internally
- In the splash animation/completion, check auth state and onboarding state, then `Navigator.pushReplacementNamed` to the appropriate route
- Import FirebaseAuth, OnboardingViewModel.hasCompletedOnboarding()
- Keep the splash screen animation/delay as-is, only change the navigation logic at the end

The navigation flow becomes:
```
SplashScreen → (checks auth + onboarding)
  → /login (if not authenticated)
  → /onboarding (if guest/authenticated but no onboarding)
  → /home (if guest/authenticated with onboarding done)

Home → /scenarios → /conversation → /feedback
Login/SignUp → (on success) → /home
```
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze lib/main.dart lib/features/splash/splash_screen.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - main.dart route table includes: /login, /signup, /forgot-password, /home, /scenarios, /conversation, /feedback
    - SplashScreen navigation logic: unauthenticated → /login, guest/auth with onboarding → /home, guest/auth without onboarding → /onboarding
    - SplashScreen no longer takes onSplashDone callback (handles routing internally)
    - New users see login screen first, not scenarios
    - Authenticated users with completed onboarding land on /home
    - flutter analyze reports 0 errors on both files
  </acceptance_criteria>
  <done>Route table updated with /login, /signup, /forgot-password, /home routes. SplashScreen routes to /login (new), /onboarding (no prefs), or /home (done). Navigation flow: Splash → Login → Home → Scenarios → Conversation → Feedback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Auth screens | Email/password fields validated client-side before Firebase Auth |
| Auth screen → Route guard | SplashScreen determines navigation based on auth + onboarding state |
| Home screen → Firestore | HomeViewModel reads user data from Firestore on mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Spoofing | Auth screens | low | accept | Client-side validation only; Firebase Auth enforces real credential checks server-side |
| T-03-06 | Info Disclosure | HomeScreen | low | accept | Displays only non-sensitive user data (name, XP, streak) — no tokens or credentials shown |
| T-03-07 | Tampering | Route guard | medium | mitigate | SplashScreen checks Firebase Auth state (not local SharedPreferences) for routing decisions |
| T-03-08 | DoS | HomeViewModel | low | accept | Firestore reads on mount are standard; Firestore quotas protect against abuse |
</threat_model>

<verification>
- `flutter analyze` reports 0 errors on all created/modified files
- LoginScreen, SignUpScreen, ForgotPasswordScreen exist with correct form fields and navigation links
- HomeScreen displays streak, goal, scenario cards, and guest banner
- Route table in main.dart includes all new routes
- SplashScreen routes correctly based on auth state
</verification>

<success_criteria>
- Auth screens (login, sign-up, forgot password) exist with full UI and form validation
- AuthViewModel handles all auth actions with loading/error states
- HomeScreen with dashboard widgets (streak ring, goal ring, scenario cards, guest banner) exists
- HomeViewModel loads data from Firestore (authenticated) or SharedPreferences (guest)
- Route table includes /login, /signup, /forgot-password, /home
- SplashScreen routes to /login (new), /onboarding (no prefs), or /home (done)
- Navigation flow: Splash → Login → Home → Scenarios → Conversation → Feedback
</success_criteria>

<output>
Create `.planning/phases/03-accounts-cloud-sync/03-02-SUMMARY.md` when done
</output>
