---
phase: 07-ui-redesign-dark-theme
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - lib/features/conversation/screens/conversation_screen.dart
  - lib/features/conversation/widgets/mic_button.dart
  - lib/features/conversation/widgets/voice_message_bubble.dart
  - lib/features/feedback/screens/feedback_screen.dart
  - lib/features/profile/screens/profile_screen.dart
  - lib/features/leaderboard/screens/leaderboard_screen.dart
  - lib/features/progress/screens/progress_screen.dart
  - lib/features/progress/widgets/level_progress.dart
  - lib/features/progress/widgets/mistake_summary.dart
  - lib/features/progress/widgets/badge_grid.dart
  - lib/features/badge/badge_popup.dart
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - All remaining screen files reference zero legacy color tokens
    - All remaining screen files reference zero AppColorProvider or ThemeModeProvider
    - Conversation mic button has gradient fill, scale spring press animation, recording state with pulsing accentCyan concentric rings
    - Voice message bubbles: user = gradient fill (accentStart -> accentEnd), AI = glass fill with glow border
    - Feedback score circle: 120px gradient fill, score counter animation (0 -> score, 800ms), confetti on 80+ score
    - Profile screen: gradient-bordered avatar circle, glass card user info, ghost sign-out button in danger color
    - Leaderboard: top 3 GlassCards with gold/silver/bronze accents, current user has accentCyan border glow
    - Progress screen: glass cards for SRS items and mistake patterns
    - flutter analyze passes with zero errors across all modified files
    - Zero legacy color token references remain in the entire lib/ directory
  artifacts:
    - 11 screen/widget files restyled to futuristic dark theme
  key_links:
    - Conversation screen mic button state machine (idle/recording/processing/speaking) wired to gradient/glow animations
    - Feedback screen score circle AnimatedBuilder driving gradient arc + counter text
    - Profile screen ThemeModeProvider import fully removed (no dark toggle UI)
    - All screens import GlassCard, GradientBackground, AppButton from Plan 01 widgets
---

<objective>
Complete the futuristic dark theme migration across all remaining screens: conversation (hero screen), feedback, profile, leaderboard, progress, and badge popup. Apply glassmorphism 2.0 components, staggered animations, and the new animation system (mic button states, score circle counter, confetti). Eliminate every last legacy color reference in the entire lib/ directory.

Purpose: Finishes the 13-screen redesign — after this plan, the entire app renders in the futuristic dark theme with zero legacy references.
Output: 11 restyled screen and widget files, zero legacy color tokens remaining project-wide.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-redesign-dark-theme/07-CONTEXT.md
@.planning/phases/07-ui-redesign-dark-theme/07-RESEARCH.md
@.planning/phases/07-ui-redesign-dark-theme/07-02-SUMMARY.md
@docs/superpowers/specs/2026-08-05-ui-redesign-design.md (sections 4.6, 4.7, 5.5, 5.6, 5.7, 5.8, 5.9, 6)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conversation Screen Redesign — Mic Button and Voice Bubbles</name>
  <files>
    lib/features/conversation/screens/conversation_screen.dart,
    lib/features/conversation/widgets/mic_button.dart,
    lib/features/conversation/widgets/voice_message_bubble.dart
  </files>
  <read_first>
    lib/features/conversation/screens/conversation_screen.dart,
    lib/features/conversation/widgets/mic_button.dart,
    lib/features/conversation/widgets/voice_message_bubble.dart,
    lib/core/theme/app_colors.dart,
    lib/core/theme/app_gradients.dart,
    lib/core/theme/app_shadows.dart,
    lib/core/theme/app_dimensions.dart,
    lib/core/theme/app_text_styles.dart,
    lib/core/widgets/app_card.dart (GlassCard),
    lib/core/widgets/app_button.dart (3-variant),
    lib/core/widgets/gradient_background.dart (MeshGradientBackground),
    docs/superpowers/specs/2026-08-05-ui-redesign-design.md (sections 4.6, 4.7, 5.5, 6.5, 6.6)
  </read_first>
  <action>
    **Global migration rules (same as Plans 01/02):**
    Replace all legacy color tokens (primaryPink, bgTop, bgBottom, accentGold, accentCoral, textDark, textMuted, shadowPink, primaryPinkDark, primaryPinkLight, surfacePrimary, surfaceSecondary, backgroundStart/Mid/End, accentPrimary/Light/Dark, accentSecondary, accentTertiary, accentDanger, borderFocus) with new semantic equivalents. Replace AppRadius.xl with lg, AppRadius.full with pill. Remove ThemeModeProvider/AppColorProvider imports. Add `import 'package:flutter_animate/flutter_animate.dart';` where needed.

    **Step 1 — conversation_screen.dart (spec 5.5) — hero screen:**
    - Root: GradientBackground (MeshGradientBackground) as scaffold background — slightly more vibrant (you can use higher opacity blobs if desired, but static mesh only per D-05)
    - Top bar: GlassCard (not Container) with frosted blur, scenario title in headingMedium, goal text in bodySmall, thin gradient progress bar (Container with AppGradients.accent, height 3, width proportional to progress)
    - Message list: ListView of voice_message_bubble widgets. Add `.animate().fadeIn(duration: 300.ms).slideX(begin: isUser ? 0.1 : -0.1)` on each new message for entrance from sender side
    - Bottom mic button area: GlassPanel (Container with surfaceGlass fill, borderTop borderSubtle, bottom padding) containing mic_button widget
    - Partial transcript display: floating GlassCard pill above mic area, textPrimary text
    - "End Conversation" button: AppButton variant primary, positioned in bottom panel
    - Remove any ThemeModeProvider watch, any AppColorProvider.current usage

    **Step 2 — mic_button.dart (spec 4.6, section 6.5):**
    - Size: 72px circle (AppSizing.micButtonSize)
    - Idle state: Container with AppGradients.accent gradient fill, white mic icon (Icons.mic), AppShadows.glowBlue boxShadow
    - Recording state: Container with accentCyan fill, red stop icon (Icons.stop), plus 3 concentric pulsing rings implemented with AnimatedBuilder + AnimationController. Rings: Container with transparent accentCyan border, expanding from 72px to 120px over 800ms cycle, 3 rings staggered. Use TickerProviderStateMixin for the AnimationController.
    - Processing state: CircularProgressIndicator with gradient decoration (use gradient stroke via DecoratedBox or just accentStart color), no mic icon
    - Speaking state: Container with surfaceGlass fill, volume icon (Icons.volume_up) in accentCyan
    - Press animation: scale 0.9 -> 1.0 spring (AnimatedScale with Curves.easeOutBack, 200ms)
    - State is driven by existing ViewModel states — no ViewModel logic changes, only visual state mapping per D-07

    **Step 3 — voice_message_bubble.dart (spec 4.7, section 6.6):**
    - User bubble (isUser = true): Container with gradient fill (AppGradients.accent), white text color, AppRadius.bubbleUser borderRadius, max width 80% of screen, aligned right. Contains transcript text + optional play/pause icon.
    - AI bubble (isUser = false): GlassCard (ClipRRect + BackdropFilter + surfaceGlass fill), textPrimary text, AppRadius.bubbleAi borderRadius, max width 80%, aligned left. Subtle glow border (AppShadows.glowCyan on the card). Contains play/pause button in accentCyan, waveform visualization bars (Container row with random-height gradient-colored bars: accentStart to accentEnd gradient), transcript text below.
    - Transcript toggle: accentCyan text with underline, tap to expand/collapse
    - Entrance animation: `.animate().fadeIn(duration: 300.ms).slideX(begin: isUser ? 0.1 : -0.1, duration: 300.ms, curve: Curves.easeOut)` — messages slide from sender's side
    - Waveform bars during playback: random height bars that animate (use flutter_animate or setState with timer — prefer flutter_animate for consistency)

    After ALL changes, run `flutter analyze` on conversation feature.
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze lib/features/conversation/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "primaryPink\|bgTop\|bgBottom\|accentGold\|accentCoral\|textDark\|textMuted\|shadowPink\|AppColorProvider\|ThemeModeProvider\|primaryPinkDark\|primaryPinkLight" lib/features/conversation/screens/*.dart lib/features/conversation/widgets/*.dart` returns 0 for each file
    - conversation_screen.dart uses GradientBackground as root, GlassCard top bar, gradient progress bar
    - mic_button.dart has 4 visual states (idle/gradient, recording/pulse rings, processing/spinner, speaking/glass), 72px circle, scale spring press
    - voice_message_bubble.dart has gradient user bubbles, glass AI bubbles, accentCyan play/pause, waveform bars
    - `flutter analyze lib/features/conversation/` passes with zero errors
  </acceptance_criteria>
  <done>Conversation screen (hero screen) fully redesigned. Mic button has 4-state gradient/glow animations with pulsing recording rings. Voice bubbles have gradient/glass styling with slide-from-sender entrance. Zero legacy references.</done>
</task>

<task type="auto">
  <name>Task 2: Feedback, Profile, Leaderboard, Progress, and Badge Screen Redesign</name>
  <files>
    lib/features/feedback/screens/feedback_screen.dart,
    lib/features/profile/screens/profile_screen.dart,
    lib/features/leaderboard/screens/leaderboard_screen.dart,
    lib/features/progress/screens/progress_screen.dart,
    lib/features/progress/widgets/level_progress.dart,
    lib/features/progress/widgets/mistake_summary.dart,
    lib/features/progress/widgets/badge_grid.dart,
    lib/features/badge/badge_popup.dart
  </files>
  <read_first>
    lib/features/feedback/screens/feedback_screen.dart,
    lib/features/profile/screens/profile_screen.dart,
    lib/features/leaderboard/screens/leaderboard_screen.dart,
    lib/features/progress/screens/progress_screen.dart,
    lib/features/progress/widgets/level_progress.dart,
    lib/features/progress/widgets/mistake_summary.dart,
    lib/features/progress/widgets/badge_grid.dart,
    lib/features/badge/badge_popup.dart,
    lib/core/theme/app_colors.dart,
    lib/core/theme/app_gradients.dart,
    lib/core/theme/app_shadows.dart,
    lib/core/theme/app_dimensions.dart,
    lib/core/theme/app_text_styles.dart,
    lib/core/widgets/app_card.dart (GlassCard),
    lib/core/widgets/app_button.dart (3-variant),
    lib/core/widgets/app_chip.dart (GlassChip),
    lib/core/widgets/stat_card.dart (glass),
    lib/core/widgets/cefr_badge.dart (cyan glow),
    lib/core/widgets/gradient_background.dart (MeshGradientBackground),
    docs/superpowers/specs/2026-08-05-ui-redesign-design.md (sections 4.8, 4.9, 5.6, 5.7, 5.8, 5.9, 6.7)
  </read_first>
  <action>
    **Global migration rules (same as Plans 01/02):**
    Apply the exact same legacy token mapping. Replace all 10 legacy color tokens with new semantic equivalents. Replace AppRadius.xl with lg, AppRadius.full with pill. Remove ThemeModeProvider/AppColorProvider imports and any theme toggle UI. Add flutter_animate import where stagger animations are used.

    **Step 1 — feedback_screen.dart (spec 5.6, section 6.7) — high animation:**
    - Root: GradientBackground (MeshGradientBackground)
    - Score circle: 120px Container, AppGradients.accent gradient fill (use Container with gradient decoration), score number centered in AppTextStyles.displayLarge (Mono, textOnAccent), glow shadow (AppShadows.glowBlue). Add counter animation: use AnimationController + Tween from 0 to score value, 800ms, Curves.easeOut. Display animated value with `.toStringAsFixed(0)` in the center.
    - Score breakdown: 3 GlassCards in a Column (Fluency, Grammar, Vocabulary). Each card: label in bodyMedium (textSecondary), value in headingLarge (textPrimary), thin gradient progress bar below. Stagger: `.animate().fadeIn(duration: 400.ms, delay: (index * 50).ms).slideY(begin: 0.1, duration: 400.ms, delay: (index * 50).ms)` on each breakdown card.
    - XP badge: GlassCard pill shape, star icon in warning color, "+XP" text in warning, bounce animation (scale 0 -> 1.1 -> 1.0 spring)
    - Grammar corrections: GlassCards with struck-through original (danger color, line-through) and corrected text (success color)
    - High score confetti (80+): Use existing `confetti` package. Import `package:confetti/confetti.dart`. Create ConfettiController, play on initState if score >= 80. ConfettiWidget at top of screen with blastDirectionality: BlastDirectionality.explosive, emissionFrequency: 0.05, numberOfParticles: 30, colors: [accentStart, accentMid, accentCyan, warning, success].
    - "Done" button: AppButton variant primary
    - Remove any ThemeModeProvider, AppColorProvider references

    **Step 2 — profile_screen.dart (spec 5.7):**
    - Root: GradientBackground
    - Avatar: Container with gradient border (2px, AppGradients.accent), borderRadius full (pill), containing circular avatar image or icon
    - User info: GlassCard with name in headingMedium, email in bodyMedium
    - Stats row: Row of 3 StatCard widgets (import from core/widgets/stat_card.dart)
    - Settings section: GlassCard with list of setting items, each row with icon + label (textSecondary) + chevron
    - Sign out button: AppButton variant ghost with danger color
    - **CRITICAL:** REMOVE the dark mode toggle UI section entirely (lines referencing ThemeModeProvider, SegmentedButton for light/dark). This is D-01 — dark only, no toggle. Delete the toggle widget/section completely.

    **Step 3 — leaderboard_screen.dart (spec 5.8):**
    - Root: GradientBackground
    - Top 3 section: 3 large GlassCards. 1st place: gold gradient border (warning color border, AppShadows.glowCyan), crown icon. 2nd: silver (textSecondary border). 3rd: bronze (accentStart border). Each card: rank number in displayLarge, username in headingMedium, XP in bodyLarge
    - Rest of list: GlassCard rows in ListView, rank number, username, XP value
    - Current user row: accentCyan border glow (GlassCard with glowColor: AppColors.accentCyan)
    - Stagger: `.animate().fadeIn().slideY()` on each list item

    **Step 4 — progress_screen.dart + widgets (spec 5.9):**
    - Root: GradientBackground
    - level_progress.dart: GlassCard with gradient progress bar (AppGradients.accent, animated width), current level text, XP to next level
    - mistake_summary.dart: GlassCard list of mistake categories, each with label (textSecondary) and count (textPrimary in Mono), danger accent for high-frequency mistakes
    - badge_grid.dart: Grid of badge items, each in a small GlassCard with icon in gradient circle, badge name in labelSmall. Earned badges: full color. Unearned: textTertiary with low opacity.
    - Stagger on all card lists

    **Step 5 — badge_popup.dart:**
    - GlassCard centered overlay/popup with gradient border glow
    - Badge icon large, centered, in gradient circle with AppShadows.glowCyan
    - Badge name in headingLarge, description in bodyMedium
    - "Awesome!" text in accentCyan
    - Scale-in animation: `.animate().scale(begin: Offset(0.9, 0.9), duration: 250.ms, curve: Curves.easeOutBack).fadeIn(duration: 250.ms)`
    - Confetti burst using confetti package (same pattern as feedback screen)

    **Step 6 — Final global verification:**
    After ALL changes across ALL files in this task, run:
    1. `grep -rn "primaryPink\|bgTop\|bgBottom\|accentGold\|accentCoral\|textDark\|textMuted\|shadowPink\|primaryPinkDark\|primaryPinkLight" lib/ --include="*.dart" | grep -v "\.g\.dart" | wc -l` — MUST return 0
    2. `grep -rn "AppColorProvider\|ThemeModeProvider\|themeModeProvider" lib/ --include="*.dart" | grep -v "\.g\.dart" | wc -l` — MUST return 0
    3. `flutter analyze` — must pass with zero errors project-wide
    4. `flutter test` — must pass (D-07: zero business logic changes)
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "primaryPink\|bgTop\|bgBottom\|accentGold\|accentCoral\|textDark\|textMuted\|shadowPink\|primaryPinkDark\|primaryPinkLight\|AppColorProvider\|ThemeModeProvider" lib/ --include="*.dart" | grep -v "\.g\.dart" | wc -l` returns 0 — zero legacy references project-wide
    - feedback_screen.dart has animated score circle (AnimationController, 800ms counter), confetti on 80+ score, staggered breakdown cards
    - profile_screen.dart has NO dark mode toggle UI, has gradient-bordered avatar, ghost sign-out button
    - leaderboard_screen.dart has top 3 gold/silver/bronze GlassCards, current user with accentCyan glow
    - progress_screen.dart widgets use GlassCard with gradient progress bars
    - badge_popup.dart has scale-in animation and confetti
    - `flutter analyze` passes with zero errors project-wide
    - `flutter test` passes (all 7 test files)
  </acceptance_criteria>
  <done>All remaining screens redesigned to futuristic dark theme. Zero legacy color references remain in the entire lib/ directory. Feedback has animated score circle and confetti. Profile has no dark mode toggle. All screens use GlassCard, GradientBackground, glassmorphism 2.0. Phase 7 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side rendering | All changes are visual screen restyling and animation, no data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | confetti performance | low | accept | confetti package already in project, minimal particle count (30), brief duration |
| T-07-06 | Tampering | AnimationController lifecycle | low | accept | Use TickerProviderStateMixin properly, dispose controllers in dispose() |
| T-07-07 | Information Disclosure | None | n/a | n/a | No data flows in screen restyling |
</threat_model>

<verification>
- `flutter analyze` — zero errors project-wide
- `flutter test` — all 7 existing test files pass (D-07: zero business logic changes)
- `grep -rn "primaryPink\|bgTop\|bgBottom\|accentGold\|accentCoral\|textDark\|textMuted\|shadowPink\|AppColorProvider\|ThemeModeProvider" lib/ --include="*.dart" | grep -v "\.g\.dart"` — returns zero lines
- Manual: open app, navigate through all 13 screens — every screen renders with dark theme, no pink/pastel colors visible, glass cards throughout, conversation mic button has gradient states, feedback score animates, profile has no dark mode toggle
</verification>

<success_criteria>
- Zero legacy color references remain in the entire lib/ directory (grep returns 0)
- Zero AppColorProvider or ThemeModeProvider references remain (grep returns 0)
- All 13 screens use MeshGradientBackground, GlassCard, and new semantic tokens
- Conversation mic button has 4-state gradient/glow animation with recording pulse rings
- Feedback score circle has animated counter and confetti on high scores
- flutter analyze passes project-wide with zero errors
- flutter test passes (no business logic changes per D-07)
</success_criteria>

<output>
Create `.planning/phases/07-ui-redesign-dark-theme/07-03-SUMMARY.md` when done
</output>
