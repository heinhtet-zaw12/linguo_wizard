---
context: phase
phase: 07-ui-redesign-dark-theme
task: 12
total_tasks: 22
status: paused
last_updated: 2026-08-06T06:10:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered in this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| GlassCard import path | GlassCard lives in `app_card.dart` not `glass_card.dart` — always verify actual file location before importing | advisory | Check `class GlassCard` location with grep before importing |
| Pre-existing analyze errors | `scaffold_with_nav_bar.dart`, `scenario_preview_card.dart` (before fix), and test files had pre-existing errors — don't confuse these with Phase 7 regressions | advisory | Run analyze on specific directories, not full project, to isolate |

<current_state>
Phase 7 (UI Redesign — Futuristic Dark Theme) has ALL 3 plans complete (07-01, 07-02, 07-03).
A brutal UI/UX design audit was completed, scanning all 30+ Dart UI files.
22 actionable items identified across 4 priority tiers (P0-P3).
No code changes were made — pure review. Next: implement the audit fixes.
See `07-UI-AUDIT.md` for the full audit report.
</current_state>

<completed_work>

- Wave 1 (07-01): Theme foundation rewrite + glassmorphism 2.0 widgets — COMPLETE
- Wave 2 (07-02): Screen redesigns batch 1 (splash, onboarding, auth, home, scenario) — 18 files — COMPLETE
- Wave 3 (07-03): Screen redesigns batch 2 (conversation, feedback, profile, leaderboard, progress, badge) — 11 files — COMPLETE
- Gap fix: deleted orphaned theme_provider.dart
- Gap fix: migrated scenario_preview_card.dart (AppCard→GlassCard, legacy tokens→AppColors)
- UI/UX Design Audit: scanned 30+ Dart UI files, produced 22-item actionable report with priority tiers

</completed_work>

<remaining_work>

**P0 — Accessibility & Trust (5 items — one-line changes):**
- Fix `textTertiary` contrast: `0xFF475569` → `0xFF64748B` in `app_colors.dart`
- Bump `surfaceGlass` opacity: `0x14151B30` → `0x24151B30` in `app_colors.dart`
- Add `Semantics(button: true, enabled: ...)` to `AppButton`
- Add disabled opacity (0.4) to `AppButton` when `onPressed == null`
- Fix loading spinner color: use `color` param instead of hardcoded `Colors.white`

**P1 — Consistency (6 items):**
- Add `DialogTheme` + `SnackBarTheme` to `AppTheme.dark`
- Replace hardcoded `BorderRadius.circular(X)` with `AppRadius` tokens in 15+ files
- Standardize horizontal padding to `AppSpacing` tokens in 10+ files
- Extract `ErrorBanner` widget (deduplicate from login, signup, create_scenario)
- Standardize back arrow icon + size across all screens
- Add `AppRadius.xl = 20` for dialogs and category tabs

**P2 — Performance & Polish (5 items):**
- Remove `BackdropFilter` from `GlassCard` and `GradientNavBar` — use solid semi-transparent color
- Replace emoji icons (`🔥`, flag emojis) with Material Icons or flag image package
- Drive conversation progress bar from actual message count
- Fix hardcoded shadow `Color(0x4D6366F1)` in `voice_message_bubble.dart`
- Fix `_NextButton` hardcoded `width: 172`

**P3 — Premium Feel (6 items):**
- Add haptic feedback on mic press, tab switch, badge earn
- Add page transition animations in router
- Add guest banner slide-in/out animation
- Add "Don't know" option to SRS item cards
- Add "Your rank" scroll-to-self on leaderboard
- Extend badge popup auto-dismiss from 4s to 6s

</remaining_work>

<decisions_made>

- Dark-only theme: removes Phase 6 light/dark toggle, ThemeModeProvider, AppColorProvider — intentional simplification
- GlassCard import is `app_card.dart` not `glass_card.dart` — Wave 1 renamed class but kept file name
- executor_model=inherit, parallelization=false, branching=none — all 3 waves ran sequentially on main tree
- Worktree isolation auto-degraded (fork-ref unknown) — ran on main working tree
- scenario_preview_card.dart was a gap miss from original plans — fixed inline

</decisions_made>

<blockers>
- None currently — all 3 plans complete, gap fixes applied
</blockers>

## Required Reading (in order)
1. docs/superpowers/specs/2026-08-05-ui-design-spec.md — design spec (single source of truth)
2. .planning/phases/07-ui-redesign-dark-theme/07-VERIFICATION.md — verification report (gaps now fixed)
3. .planning/STATE.md — current project state

## Infrastructure State
- Flutter analyze: 34 issues — all pre-existing (scaffold_with_nav_bar.dart, test files, seed_scenarios.dart)
- Phase 7 source files: all clean, zero legacy color references in all 29 migrated files
- gsd-tools.cjs location: ~/.claude/gsd-core/bin/gsd-tools.cjs

<context>
Phase 7 all 3 plans are complete. A comprehensive UI/UX design audit was performed as the final step.
The audit found the theme architecture is solid but 40% of screens bypass tokens with hardcoded values.
Accessibility is borderline broken (textTertiary fails WCAG, AppButton has no semantics).
BackdropFilter on 40+ cards is a performance concern on mid-range devices.
P0 fixes are one-line changes in app_colors.dart and app_button.dart.
Overall score: 6.2/10 — strong foundation, poor execution consistency.
</context>

<next_action>
Begin implementing P0 fixes: update `app_colors.dart` (textTertiary + surfaceGlass), update `app_button.dart` (Semantics, disabled state, spinner color). Then proceed to P1 consistency fixes. See `07-UI-AUDIT.md` for full details.
</next_action>
