---
phase: 07-ui-redesign-dark-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/theme/app_colors.dart
  - lib/core/theme/app_theme.dart
  - lib/core/theme/app_text_styles.dart
  - lib/core/theme/app_dimensions.dart
  - lib/core/theme/app_shadows.dart
  - lib/core/theme/app_gradients.dart
  - lib/core/widgets/app_card.dart
  - lib/core/widgets/app_button.dart
  - lib/core/widgets/app_chip.dart
  - lib/core/widgets/app_nav_bar.dart
  - lib/core/widgets/gradient_background.dart
  - lib/core/widgets/app_text_field.dart
  - lib/core/widgets/stat_card.dart
  - lib/core/widgets/cefr_badge.dart
  - lib/core/widgets/info_row.dart
  - lib/core/providers/theme_provider.dart
  - lib/main.dart
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - AppColors is a flat const class with no AppColorProvider delegation — all getters are direct Color constants
    - AppRadius tokens are sm=12, md=16, lg=24, pill=9999 — no xl or full
    - AppShadows includes glowBlue and glowCyan colored box-shadow tokens
    - AppGradients provides accent, accentCyan, and surface gradient builders
    - MeshGradientBackground renders 3-4 static RadialGradient blobs on a Stack
    - GlassCard uses ClipRRect + BackdropFilter(sigmaX/Y: 20) with surfaceGlass fill
    - AppButton has primary (gradient+glow), secondary (glass+accentCyan border), ghost (text accentCyan) variants with scale 0.97 press animation
    - GradientNavBar has glass panel with gradient indicator pill for active tab
    - ThemeModeProvider is deleted from codebase — no dark mode toggle exists
    - flutter analyze passes with zero errors after all changes
  artifacts:
    - lib/core/theme/app_colors.dart (rewritten as flat dark-only palette)
    - lib/core/theme/app_gradients.dart (new file)
    - lib/core/theme/app_theme.dart (simplified to single dark ThemeData)
    - lib/core/widgets/app_card.dart (rewritten as GlassCard)
    - lib/core/widgets/gradient_background.dart (rewritten as MeshGradientBackground)
    - lib/core/providers/theme_provider.dart (deleted)
  key_links:
    - app_colors.dart constants consumed by all widgets and screens without provider delegation
    - GlassCard BackdropFilter correctly wrapped in ClipRRect to prevent blur bleed
    - AppButton GestureDetector -> AnimatedScale -> _buildPrimary/_buildSecondary/_buildGhost chain
    - GradientNavBar active indicator reads accentStart/accentEnd gradient tokens
---

<objective>
Rewrite the entire theme foundation (colors, typography, dimensions, shadows, gradients) as a single flat dark-only palette, rewrite all 9 core widgets to glassmorphism 2.0 components, and delete the AppColorProvider/ThemeModeProvider light/dark toggle infrastructure. This creates the design system that all 13 screen redesigns will consume.

Purpose: Establishes the futuristic dark theme foundation — without this, no screen can be redesigned.
Output: Rewritten theme token files, new app_gradients.dart, rewritten core widgets, deleted provider files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-redesign-dark-theme/07-CONTEXT.md
@.planning/phases/07-ui-redesign-dark-theme/07-RESEARCH.md
@docs/superpowers/specs/2026-08-05-ui-redesign-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme Foundation Rewrite + Provider Deletion</name>
  <files>
    lib/core/theme/app_colors.dart,
    lib/core/theme/app_theme.dart,
    lib/core/theme/app_text_styles.dart,
    lib/core/theme/app_dimensions.dart,
    lib/core/theme/app_shadows.dart,
    lib/core/theme/app_gradients.dart (new),
    lib/core/providers/theme_provider.dart (delete),
    lib/main.dart
  </files>
  <read_first>
    lib/core/theme/app_colors.dart,
    lib/core/theme/app_theme.dart,
    lib/core/theme/app_text_styles.dart,
    lib/core/theme/app_dimensions.dart,
    lib/core/theme/app_shadows.dart,
    lib/main.dart,
    lib/core/providers/theme_provider.dart,
    docs/superpowers/specs/2026-08-05-ui-redesign-design.md (sections 2-3)
  </read_first>
  <action>
    **Step 1 — Rewrite app_colors.dart (per D-01, D-02):** Delete AppColorTokens abstract class, LightColors, DarkColors, and AppColorProvider entirely. Replace the entire file with a single flat const class `AppColors` containing direct Color constant getters for the new palette per spec section 2: surfaceBase (#0A0E1A), surface0 (#0F1424), surface1 (#151B30), surface2 (#1C2340), surface3 (#242D50), accentStart (#6366F1), accentMid (#8B5CF6), accentEnd (#A78BFA), accentCyan (#22D3EE), accentCyanGlow (#06B6D4), textPrimary (#F1F5F9), textSecondary (#94A3B8), textTertiary (#475569), textOnAccent (#FFFFFF), success (#34D399), warning (#FBBF24), danger (#F87171), borderSubtle (#1E293B), borderGlow (#6366F140), surfaceGlass (surface1 at 0.08 opacity using `Color(0x14151B30)`), glassBlur (20.0), glassBorderOpacity (0.12). Remove ALL legacy const fields (primaryPink, primaryPinkDark, primaryPinkLight, bgTop, bgBottom, accentGold, accentCoral, textDark, textMuted, textLight, shadowPink). The class constructor is `AppColors._()` private.

    **Step 2 — Create app_gradients.dart (per D-11):** New file at `lib/core/theme/app_gradients.dart`. Create `AppGradients` utility class with static LinearGradient getters: `accent` returns LinearGradient from accentStart through accentMid to accentEnd (alignment left-to-right), `accentCyan` returns LinearGradient from accentCyan to accentStart, `surface` returns LinearGradient from surface0 to surface1. Each getter is a const or static final LinearGradient.

    **Step 3 — Rewrite app_theme.dart (per D-01):** Remove all light mode logic. Remove AppTheme.updateColorProvider call. Remove ThemeMode switching. Simplify to a single `ThemeData get darkTheme` that uses the new AppColors constants directly for all ThemeData properties: scaffoldBackgroundColor = surfaceBase, colorScheme = ColorScheme.dark with primary=accentStart, secondary=accentMid, surface=surface1, error=danger, onPrimary=textOnAccent, onSurface=textPrimary. Set textTheme using new AppTextStyles. Set fontFamily to 'Inter'. Hardcode `themeMode: ThemeMode.dark` in MaterialApp.

    **Step 4 — Rewrite app_text_styles.dart:** Update all text style color defaults to use the new semantic tokens. Per spec section 3.1: displayLarge uses textPrimary (Mono 32/700), displayMedium uses textPrimary (Jakarta 24/700), headingLarge uses textPrimary (Jakarta 20/600), headingMedium uses textPrimary (Jakarta 17/600), headingSmall uses textPrimary (Jakarta 15/600), bodyLarge uses textSecondary (Inter 16/400), bodyMedium uses textSecondary (Inter 14/400), bodySmall uses textTertiary (Inter 13/400), labelLarge uses textPrimary (Inter 14/500), labelMedium uses textSecondary (Inter 12/500), labelSmall uses textTertiary (Jakarta 11/600). Keep the same GoogleFonts import pattern. Remove any references to AppColors.textDark or AppColors.textMuted.

    **Step 5 — Rewrite app_dimensions.dart (per D-08):** Update AppRadius: sm = 12 (was 8), md = 16 (was 12), lg = 24 (was 16). Delete AppRadius.xl. Rename AppRadius.full to AppRadius.pill (value 9999). Update bubbleUser and bubbleAi radii from 16/4 to 24/4 (using lg radius). Keep AppSpacing and AppSizing unchanged.

    **Step 6 — Rewrite app_shadows.dart (per D-10):** Replace the existing shadow system. Define elevation1 = [BoxShadow(color: Color(0x4D000000), blurRadius: 8, offset: Offset(0, 2))], elevation2 = [BoxShadow(color: Color(0x66000000), blurRadius: 16, offset: Offset(0, 4))], elevation3 = [BoxShadow(color: Color(0x80000000), blurRadius: 32, offset: Offset(0, 8))]. Add glowBlue = [BoxShadow(color: Color(0x4D6366F1), blurRadius: 20, spreadRadius: -2)] and glowCyan = [BoxShadow(color: Color(0x4022D3EE), blurRadius: 16, spreadRadius: -2)]. All as static const List<BoxShadow>.

    **Step 7 — Delete theme_provider.dart and update main.dart (per D-01, D-09):** Delete `lib/core/providers/theme_provider.dart` entirely. In `lib/main.dart`: remove the `ref.watch(themeModeProvider)` line, remove `AppTheme.updateColorProvider` call if present, hardcode `themeMode: ThemeMode.dark` in MaterialApp widget. Remove the import of theme_provider.dart.

    **CRITICAL:** After ALL changes in this task, run `flutter analyze` — it WILL show compilation errors because screens still reference deleted tokens (primaryPink, bgTop, etc.) and deleted classes (AppColorProvider, ThemeModeProvider). These errors are expected and will be fixed in Task 2. The ONLY errors allowed are those caused by this task's token renames/deletions — no other errors.
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze lib/core/theme/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app_colors.dart contains zero references to AppColorProvider, LightColors, DarkColors, AppColorTokens
    - app_colors.dart contains zero legacy const fields (primaryPink, bgTop, bgBottom, accentGold, accentCoral, textDark, textMuted, shadowPink)
    - app_colors.dart has surfaceBase, surface0-surface3, accentStart, accentMid, accentEnd, accentCyan, accentCyanGlow, textPrimary-tertiary, textOnAccent, success, warning, danger, borderSubtle, borderGlow, surfaceGlass as const getters
    - AppRadius has sm=12, md=16, lg=24, pill=9999 — no xl, no full
    - app_gradients.dart exists with accent, accentCyan, surface LinearGradient getters
    - AppShadows has elevation1/2/3, glowBlue, glowCyan
    - theme_provider.dart file no longer exists
    - main.dart contains `themeMode: ThemeMode.dark` and no reference to themeModeProvider
    - `grep -r "AppColorProvider\|ThemeModeProvider\|primaryPink\|bgTop\|bgBottom\|accentGold\|accentCoral\|textDark\|textMuted\|shadowPink" lib/core/theme/ lib/core/providers/ lib/main.dart` returns zero matches
    - `flutter analyze lib/core/theme/` passes with zero errors
  </acceptance_criteria>
  <done>Theme foundation fully rewritten as dark-only flat palette, provider infrastructure deleted, AppGradients utility created, radius tokens shifted, glow shadows added. Screen/widget compilation errors expected (fixed in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Core Widgets Rewrite — Glassmorphism 2.0 Components</name>
  <files>
    lib/core/widgets/app_card.dart,
    lib/core/widgets/app_button.dart,
    lib/core/widgets/app_chip.dart,
    lib/core/widgets/app_nav_bar.dart,
    lib/core/widgets/gradient_background.dart,
    lib/core/widgets/app_text_field.dart,
    lib/core/widgets/stat_card.dart,
    lib/core/widgets/cefr_badge.dart,
    lib/core/widgets/info_row.dart
  </files>
  <read_first>
    lib/core/widgets/app_card.dart,
    lib/core/widgets/app_button.dart,
    lib/core/widgets/app_chip.dart,
    lib/core/widgets/app_nav_bar.dart,
    lib/core/widgets/gradient_background.dart,
    lib/core/widgets/app_text_field.dart,
    lib/core/widgets/stat_card.dart,
    lib/core/widgets/cefr_badge.dart,
    lib/core/widgets/info_row.dart,
    lib/core/theme/app_colors.dart (the rewritten version from Task 1),
    lib/core/theme/app_gradients.dart,
    lib/core/theme/app_shadows.dart,
    lib/core/theme/app_dimensions.dart,
    docs/superpowers/specs/2026-08-05-ui-redesign-design.md (sections 4-6)
  </read_first>
  <action>
    **Step 1 — Rewrite app_card.dart as GlassCard (per D-03):** Replace entire file contents. Create `GlassCard` StatelessWidget with params: `child` (required), `padding` (default AppSpacing.all4), `borderRadius` (default AppRadius.md), `glowColor` (optional Color), `elevation` (int, default 1). Build: ClipRRect(borderRadius) wrapping BackdropFilter(filter: ImageFilter.blur(sigmaX: 20, sigmaY: 20), child: Container with padding, BoxDecoration(color: AppColors.surfaceGlass, borderRadius, border: Border.all(color: glowColor ?? AppColors.borderSubtle, width: 0.5), boxShadow: [...AppShadows.elevation1 (or elevation2/3 based on elevation param), if glowColor != null BoxShadow(color: glowColor!, blurRadius: 20, spreadRadius: -2)]), child: child). Import dart:ui for ImageFilter. Add `import 'dart:ui';` at top.

    **Step 2 — Rewrite app_button.dart with 3 variants (per D-13):** Replace entire file. Create `AppButtonVariant` enum: primary, secondary, ghost. Create `AppButton` StatefulWidget with params: label (String), onPressed (VoidCallback?), variant (default primary), icon (IconData?), isLoading (bool, default false). State manages `_scale` double (1.0 default). GestureDetector with onTapDown sets _scale=0.97, onTapUp/onTapCancel sets _scale=1.0, onTap calls widget.onPressed. Child is AnimatedScale(scale: _scale, duration: 100ms, curve: Curves.easeOut, child: _buildButton()). _buildPrimary: Container height 52, borderRadius AppRadius.md, decoration BoxDecoration with gradient AppGradients.accent, boxShadow AppShadows.glowBlue, child Center with Text(label, style: AppTextStyles.labelLarge with textOnAccent color). _buildSecondary: Container height 52, glass fill (AppColors.surfaceGlass), border accentCyan 1px, child with accentCyan text. _buildGhost: No background, just Text(label, color: accentCyan). If isLoading, show CircularProgressIndicator代替text in primary/secondary.

    **Step 3 — Rewrite app_chip.dart as GlassChip:** Replace entire file. Create `GlassChip` StatelessWidget with params: label (String), selected (bool), onTap (VoidCallback?). Selected state: gradient fill (AppGradients.accent), glow shadow (AppShadows.glowCyan), white text. Unselected state: glass fill (AppColors.surfaceGlass), borderSubtle border, textSecondary text. Shape: pill (AppRadius.pill), height: AppSizing.chipHeight (36). Use Container with AnimatedContainer for selection transition (200ms). Text style: AppTextStyles.labelMedium.

    **Step 4 — Rewrite gradient_background.dart as MeshGradientBackground:** Replace entire file. Class name stays `GradientBackground` to avoid breaking all 13 screen imports (per Pitfall 6 in research). Change implementation: Stack with [Container(color: AppColors.surfaceBase), Positioned blobs]. Blob 1: top-left (-80, -40), size 280, accentStart at 0.07 opacity. Blob 2: center-right (200, -60), size 320, accentMid at 0.05. Blob 3: bottom-center (-100, 80), size 260, accentCyan at 0.06. Each blob: Container with BoxDecoration(shape: BoxShape.circle, gradient: RadialGradient(colors: [color.withValues(alpha: opacity), Colors.transparent])). Last child is `this.child`. Private helper `_blob(double size, Color color, double opacity)`.

    **Step 5 — Rewrite app_nav_bar.dart as GradientNavBar:** Replace entire file. Create `GradientNavBar` StatefulWidget with params: currentIndex (int), onTap (ValueChanged<int>), items (List<GradientNavItem> where GradientNavItem has icon, label). Build: Container height AppSizing.navBarHeight, glass fill (AppColors.surfaceGlass), top border 0.5px borderSubtle, BackdropFilter(blur:20) wrapped in ClipRRect. Row of items: active item has gradient indicator pill (Container with AppRadius.pill, gradient AppGradients.accent, width 48 height 32, containing icon in white). Inactive items: icon in textTertiary. OnTap forwards to parent. Use GestureDetector or InkWell per item.

    **Step 6 — Update remaining widgets (app_text_field, stat_card, cefr_badge, info_row):** In app_text_field.dart: replace container background with AppColors.surfaceGlass fill, add accentCyan focus border glow (BorderSide(color: AppColors.accentCyan) when focused), use AppRadius.md, update text colors to textPrimary/textSecondary. In stat_card.dart: wrap in GlassCard (import glass card), use gradient icon circle (small Container with AppGradients.accent, borderRadius full, containing icon), value text in AppTextStyles.displayMedium (Mono), label in AppTextStyles.labelMedium. In cefr_badge.dart: pill shape (AppRadius.pill), glass fill, accentCyan border (1px), text in accentCyan, JetBrains Mono 11px. In info_row.dart: replace hardcoded colors with semantic tokens (textPrimary for value, textSecondary for label).

    **CRITICAL:** Do NOT rename files (app_card.dart stays app_card.dart, gradient_background.dart stays gradient_background.dart). Change class names inside (AppCard -> GlassCard, GradientBackground stays GradientBackground). Update any internal imports. After ALL changes, run `flutter analyze` to verify zero errors in the widgets directory.
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze lib/core/widgets/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GlassCard class exists in app_card.dart with BackdropFilter(sigmaX: 20, sigmaY: 20) wrapped in ClipRRect
    - AppButton has primary/secondary/ghost variants with GestureDetector -> AnimatedScale(scale: _scale) press animation
    - GlassChip class exists with selected (gradient fill + glow) and unselected (glass fill) states
    - GradientBackground class renders static mesh with 3 RadialGradient blobs at 5-8% opacity on surfaceBase
    - GradientNavBar has glass panel with gradient indicator pill for active tab
    - app_text_field.dart uses AppColors.surfaceGlass for fill and accentCyan for focus border
    - stat_card.dart uses GlassCard wrapper with gradient icon circle
    - cefr_badge.dart uses pill shape with accentCyan border
    - `grep -c "AppColorProvider\|primaryPink\|bgTop\|bgBottom\|AppRadius\.xl\|AppRadius\.full" lib/core/widgets/*.dart` returns 0 for each file
    - `flutter analyze lib/core/widgets/` passes with zero errors
    - All widget files import from the new app_colors.dart flat constants (not from provider)
  </acceptance_criteria>
  <done>All 9 core widgets restyled to glassmorphism 2.0. GlassCard, GlassChip, GradientNavBar, MeshGradientBackground (as GradientBackground class), 3-variant AppButton with press animation, glass text field, gradient stat card, cyan CEFR badge. Ready for screen redesigns in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side rendering | All changes are visual-only theme files, no data flows across trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | BackdropFilter performance | low | accept | Glass blur at sigma 20; profile on device; tune if needed |
| T-07-02 | Information Disclosure | None | n/a | n/a | No data flows in theme layer changes |
</threat_model>

<verification>
- `flutter analyze lib/core/theme/` — zero errors
- `flutter analyze lib/core/widgets/` — zero errors
- `flutter test` — all 7 existing test files pass (no business logic changed per D-07)
- Manual: open app on device/simulator, verify dark background with blue-violet tones, glass cards, gradient nav bar visible
</verification>

<success_criteria>
- All theme files contain only dark-only flat const values (no provider, no light/dark switch)
- All 9 core widgets use glassmorphism 2.0 (BackdropFilter, glass fill, glow shadows)
- AppColorProvider and ThemeModeProvider are fully deleted
- flutter analyze passes on theme + widgets directories
- Zero legacy color token references remain in core/theme/ or core/widgets/
</success_criteria>

<output>
Create `.planning/phases/07-ui-redesign-dark-theme/07-01-SUMMARY.md` when done
</output>
