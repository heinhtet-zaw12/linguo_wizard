---
phase: 07-ui-redesign-dark-theme
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/features/splash/splash_screen.dart
  - lib/features/onboarding/screens/onboarding_screen.dart
  - lib/features/onboarding/widgets/language_step.dart
  - lib/features/onboarding/widgets/cefr_step.dart
  - lib/features/onboarding/widgets/goal_step.dart
  - lib/features/auth/screens/login_screen.dart
  - lib/features/auth/screens/signup_screen.dart
  - lib/features/auth/screens/forgot_password_screen.dart
  - lib/features/home/screens/home_screen.dart
  - lib/features/home/widgets/streak_ring.dart
  - lib/features/home/widgets/goal_ring.dart
  - lib/features/home/widgets/daily_challenge_card.dart
  - lib/features/home/widgets/scenario_cards.dart
  - lib/features/home/widgets/guest_banner.dart
  - lib/features/scenario_selection/screens/scenario_selection_screen.dart
  - lib/features/scenario_selection/widgets/scenario_card.dart
  - lib/features/scenario_selection/screens/create_scenario_screen.dart
  - lib/features/srs/screens/pre_scenario_review_screen.dart
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - All screen files reference zero legacy color tokens (primaryPink, bgTop, bgBottom, accentGold, accentCoral, textDark, textMuted, shadowPink, primaryPinkDark, primaryPinkLight)
    - All screen files reference zero AppColorProvider or ThemeModeProvider
    - All screen files use AppColors semantic tokens for all color values
    - All card containers use GlassCard instead of raw Container/Card with manual decoration
    - Splash screen renders on MeshGradientBackground with centered logo and gradient spinner
    - Onboarding uses PageView with GlassCard step containers and primary gradient Next button
    - Auth screens use glass-filled form fields with accentCyan focus glow
    - Home dashboard has staggered flutter_animate card entrance (fadeIn + slideY, 50ms delay)
    - Scenario selection grid uses GlassCards with gradient top stripe
    - flutter analyze passes with zero errors across all modified screen files
  artifacts:
    - 18 screen/widget files restyled to futuristic dark theme
  key_links:
    - All screens import GlassCard (app_card.dart) and GradientBackground (gradient_background.dart)
    - All screens use AppColors.surfaceBase/surface0 for backgrounds, not hardcoded Color values
    - flutter_animate .animate().fadeIn().slideY() applied to list/grid card items with index-based delay
---

<objective>
Migrate all 18 screen and widget files across splash, onboarding, auth, home, and scenario features from legacy color references to the new futuristic dark theme tokens. Every card uses GlassCard, every screen uses MeshGradientBackground, and card lists use staggered flutter_animate entrance animations.

Purpose: Applies the glassmorphism 2.0 design system to the first 7 of 13 screens — the onboarding/auth entry flow and the home/scenario browsing flow.
Output: 18 restyled screen and widget files, zero legacy color references in these features.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-redesign-dark-theme/07-CONTEXT.md
@.planning/phases/07-ui-redesign-dark-theme/07-RESEARCH.md
@.planning/phases/07-ui-redesign-dark-theme/07-01-SUMMARY.md
@docs/superpowers/specs/2026-08-05-ui-redesign-design.md (sections 5, 6)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth, Onboarding, and Splash Screen Redesign</name>
  <files>
    lib/features/splash/splash_screen.dart,
    lib/features/onboarding/screens/onboarding_screen.dart,
    lib/features/onboarding/widgets/language_step.dart,
    lib/features/onboarding/widgets/cefr_step.dart,
    lib/features/onboarding/widgets/goal_step.dart,
    lib/features/auth/screens/login_screen.dart,
    lib/features/auth/screens/signup_screen.dart,
    lib/features/auth/screens/forgot_password_screen.dart
  </files>
  <read_first>
    lib/features/splash/splash_screen.dart,
    lib/features/onboarding/screens/onboarding_screen.dart,
    lib/features/onboarding/widgets/language_step.dart,
    lib/features/onboarding/widgets/cefr_step.dart,
    lib/features/onboarding/widgets/goal_step.dart,
    lib/features/auth/screens/login_screen.dart,
    lib/features/auth/screens/signup_screen.dart,
    lib/features/auth/screens/forgot_password_screen.dart,
    lib/core/theme/app_colors.dart (rewritten version from Plan 01),
    lib/core/widgets/app_card.dart (GlassCard from Plan 01),
    lib/core/widgets/app_button.dart (3-variant AppButton from Plan 01),
    lib/core/widgets/app_chip.dart (GlassChip from Plan 01),
    lib/core/widgets/gradient_background.dart (MeshGradientBackground from Plan 01),
    lib/core/widgets/app_text_field.dart (glass text field from Plan 01),
    lib/core/theme/app_gradients.dart,
    lib/core/theme/app_shadows.dart,
    lib/core/theme/app_dimensions.dart,
    lib/core/theme/app_text_styles.dart,
    docs/superpowers/specs/2026-08-05-ui-redesign-design.md (sections 5.0, 5.1, 5.2)
  </read_first>
  <action>
    **Global migration rules for ALL screen files in this task:**
    - Replace ALL `AppColors.primaryPink` with `AppColors.accentStart` or `AppColors.accentMid`
    - Replace ALL `AppColors.primaryPinkDark` with `AppColors.accentStart`
    - Replace ALL `AppColors.primaryPinkLight` with `AppColors.accentEnd`
    - Replace ALL `AppColors.textDark` with `AppColors.textPrimary`
    - Replace ALL `AppColors.textMuted` with `AppColors.textSecondary`
    - Replace ALL `AppColors.bgTop` with `AppColors.surface0`
    - Replace ALL `AppColors.bgBottom` with `AppColors.surface1`
    - Replace ALL `AppColors.accentGold` with `AppColors.warning`
    - Replace ALL `AppColors.accentCoral` with `AppColors.danger`
    - Replace ALL `AppColors.shadowPink` with removal (use AppShadows.elevation1 instead)
    - Replace ALL `AppColors.surfacePrimary` with `AppColors.surfaceBase`
    - Replace ALL `AppColors.surfaceSecondary` with `AppColors.surface1`
    - Replace ALL `AppColors.backgroundStart/Mid/End` with `AppColors.surface0/surface1/surface2`
    - Replace ALL `AppColors.accentPrimary` with `AppColors.accentStart`
    - Replace ALL `AppColors.accentPrimaryLight` with `AppColors.accentEnd`
    - Replace ALL `AppColors.accentPrimaryDark` with `AppColors.accentMid`
    - Replace ALL `AppColors.accentSecondary` with `AppColors.warning`
    - Replace ALL `AppColors.accentTertiary` with `AppColors.accentCyan`
    - Replace ALL `AppColors.accentDanger` with `AppColors.danger`
    - Replace ALL `AppColors.borderFocus` with `AppColors.accentCyan`
    - Replace ALL `AppRadius.xl` with `AppRadius.lg`
    - Replace ALL `AppRadius.full` with `AppRadius.pill`
    - Remove ALL `import` of `theme_provider.dart` or any reference to `themeModeProvider`
    - Wrap all card-like Containers in GlassCard where they serve as cards
    - Use MeshGradientBackground (via GradientBackground class) as root scaffold background where GradientBackground was already used
    - Add `import 'package:flutter_animate/flutter_animate.dart';` where card stagger animations are added

    **Step 1 — splash_screen.dart (spec 5.0):**
    - Background: GradientBackground (MeshGradientBackground) fills entire screen
    - Center: App logo with scale-in animation (scale 0.0 -> 1.0, 400ms, Curves.easeOutBack)
    - Below logo: gradient spinner (CircularProgressIndicator with AppGradients.accent as decoration)
    - Remove any legacy color references

    **Step 2 — onboarding_screen.dart + 3 step widgets (spec 5.1):**
    - Background: GradientBackground fills screen
    - PageView with 3 pages (language, CEFR, goal)
    - Each step: GlassCard floating over background, centered content
    - Progress dots: active dot uses gradient fill (AppGradients.accent), inactive uses glass fill (AppColors.surfaceGlass)
    - "Next" button: AppButton with variant: AppButtonVariant.primary
    - Page transition: subtle scale (0.95 -> 1.0, 200ms)
    - In language_step.dart, cefr_step.dart, goal_step.dart: replace all legacy colors, use GlassChip for selectable options, accentCyan for selected state

    **Step 3 — Auth screens (spec 5.2):**
    - login_screen.dart: Centered layout, root Scaffold with GradientBackground. Form container: GlassCard with padding. Logo centered at top. Email/password fields: AppTextField (glass fill, accentCyan focus glow). "Sign In" button: AppButton variant primary. "Continue with Google" button: AppButton variant secondary. "Continue as Guest" button: AppButton variant ghost. Error text: danger color. Card entrance: add `.animate().fadeIn(duration: 400.ms).slideY(begin: 0.1, duration: 400.ms)` on the GlassCard.
    - signup_screen.dart: Same glass card pattern as login. Name, email, password fields. Primary button for "Create Account". Secondary for Google. Ghost for "Already have an account".
    - forgot_password_screen.dart: GlassCard with email field and primary "Send Reset Link" button. Ghost "Back to Login".
    - For ALL auth screens: remove any `AppColorProvider` import, replace all legacy color tokens per the mapping above, remove ThemeModeProvider import if present.

    After ALL changes, run `flutter analyze` on the modified files to confirm zero errors.
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze lib/features/splash/ lib/features/onboarding/ lib/features/auth/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "primaryPink\|bgTop\|bgBottom\|accentGold\|accentCoral\|textDark\|textMuted\|shadowPink\|AppColorProvider\|ThemeModeProvider\|primaryPinkDark\|primaryPinkLight" lib/features/splash/splash_screen.dart lib/features/onboarding/screens/onboarding_screen.dart lib/features/onboarding/widgets/*.dart lib/features/auth/screens/*.dart` returns 0 for each file
    - splash_screen.dart uses GradientBackground as root, has scale-in logo animation
    - onboarding_screen.dart has PageView with GlassCard step containers and gradient progress dots
    - All 3 auth screens use GlassCard form containers, AppTextField with glass fill, AppButton with correct variants
    - All auth screens import GlassCard from app_card.dart (not old AppCard)
    - `flutter analyze lib/features/splash/ lib/features/onboarding/ lib/features/auth/` passes with zero errors
  </acceptance_criteria>
  <done>All 8 splash/onboarding/auth screen and widget files restyled to futuristic dark theme. Zero legacy color references. Glass cards, gradient buttons, mesh background applied throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Home Dashboard and Scenario Selection Screen Redesign</name>
  <files>
    lib/features/home/screens/home_screen.dart,
    lib/features/home/widgets/streak_ring.dart,
    lib/features/home/widgets/goal_ring.dart,
    lib/features/home/widgets/daily_challenge_card.dart,
    lib/features/home/widgets/scenario_cards.dart,
    lib/features/home/widgets/guest_banner.dart,
    lib/features/scenario_selection/screens/scenario_selection_screen.dart,
    lib/features/scenario_selection/widgets/scenario_card.dart,
    lib/features/scenario_selection/screens/create_scenario_screen.dart,
    lib/features/srs/screens/pre_scenario_review_screen.dart
  </files>
  <read_first>
    lib/features/home/screens/home_screen.dart,
    lib/features/home/widgets/streak_ring.dart,
    lib/features/home/widgets/goal_ring.dart,
    lib/features/home/widgets/daily_challenge_card.dart,
    lib/features/home/widgets/scenario_cards.dart,
    lib/features/home/widgets/guest_banner.dart,
    lib/features/scenario_selection/screens/scenario_selection_screen.dart,
    lib/features/scenario_selection/widgets/scenario_card.dart,
    lib/features/scenario_selection/screens/create_scenario_screen.dart,
    lib/features/srs/screens/pre_scenario_review_screen.dart,
    lib/core/theme/app_colors.dart (rewritten from Plan 01),
    lib/core/widgets/app_card.dart (GlassCard),
    lib/core/widgets/app_button.dart (3-variant),
    lib/core/widgets/app_chip.dart (GlassChip),
    lib/core/widgets/gradient_background.dart (MeshGradientBackground),
    lib/core/widgets/app_text_field.dart (glass),
    lib/core/widgets/stat_card.dart (glass),
    lib/core/widgets/cefr_badge.dart (cyan glow),
    lib/core/theme/app_gradients.dart,
    lib/core/theme/app_shadows.dart,
    lib/core/theme/app_text_styles.dart,
    docs/superpowers/specs/2026-08-05-ui-redesign-design.md (sections 5.3, 5.4, 5.4a, 5.4b)
  </read_first>
  <action>
    **Global migration rules (same as Task 1):**
    Apply the exact same legacy token mapping from Task 1 to every file in this task. Replace primaryPink/bgTop/bgBottom/accentGold/accentCoral/textDark/textMuted/shadowPink/primaryPinkDark/primaryPinkLight with new semantic equivalents. Replace surfacePrimary with surfaceBase, backgroundStart/Mid/End with surface0/1/2, accentPrimary with accentStart, etc. Replace AppRadius.xl with lg, AppRadius.full with pill. Remove ThemeModeProvider/AppColorProvider imports. Add `import 'package:flutter_animate/flutter_animate.dart';` where stagger animations are used.

    **Step 1 — home_screen.dart (spec 5.3) — most complex screen:**
    - Root: GradientBackground (MeshGradientBackground) filling scaffold
    - Welcome header: Text with AppTextStyles.displayMedium (screen title) + bodyLarge (greeting)
    - Streak card: GlassCard containing streak_ring widget
    - Daily goal ring: GlassCard containing goal_ring widget
    - Daily Challenge hero card: GlassCard with AppShadows.glowCyan, larger than other cards, "Today's Challenge" label badge with accentCyan border
    - Recommended scenarios section: horizontal ListView of scenario_cards
    - Guest banner (if guest): GlassCard with accentCyan border
    - CRITICAL: Add staggered entrance animation to ALL cards — wrap the Column/List children so each card gets `.animate().fadeIn(duration: 400.ms, delay: (index * 50).ms).slideY(begin: 0.1, duration: 400.ms, delay: (index * 50).ms)`
    - Remove any ThemeModeProvider watch or AppColorProvider.current usage

    **Step 2 — Home widgets (streak_ring, goal_ring, daily_challenge_card, scenario_cards, guest_banner):**
    - streak_ring.dart: Replace accentGold with AppColors.warning, use AppTextStyles for count text, wrap decorative elements in appropriate tokens
    - goal_ring.dart: Replace accentPrimary with accentStart/accentMid gradient, use warning for XP text
    - daily_challenge_card.dart: Use GlassCard wrapper, accentCyan for "Today's Challenge" badge border, gradient for title text
    - scenario_cards.dart: Use GlassCard for each scenario card, gradient top stripe (Container with AppGradients.accent, height 4, borderRadius top only), GlassChip for CEFR badge, staggered flutter_animate entrance
    - guest_banner.dart: GlassCard with accentCyan border (borderSide: BorderSide(color: AppColors.accentCyan)), textPrimary text, ghost "Sign Up" button

    **Step 3 — Scenario selection + create + pre-review screens (spec 5.4, 5.4a, 5.4b):**
    - scenario_selection_screen.dart: GradientBackground root, search bar with glass-filled Container (AppColors.surfaceGlass fill, AppRadius.md, textPrimary text), category tabs using GlassChip row (horizontal ListView), CEFR filter chips also GlassChip, scenario grid: SliverGrid with scenario_card widgets. Add `.animate().fadeIn().slideY()` stagger on grid items with 50ms delay. Infinite scroll glass loading spinner.
    - scenario_card.dart: GlassCard with gradient top stripe (4px Container, AppGradients.accent), icon in gradient circle, title in headingMedium, CefrBadge, category text in bodySmall. Completed state: check icon + "Twist" Ghost button in accentCyan.
    - create_scenario_screen.dart: GradientBackground root, GlassCard form container. Title field: AppTextField (glass fill, accentCyan focus). Description field: AppTextField multiline. Difficulty selector: 3 GlassChips (Easy/Medium/Hard) in a Row. "Generate" button: AppButton variant primary. Preview card: GlassCard showing generated scenario content. "Save" button: AppButton variant primary, disabled when no preview. "Cancel": AppButton variant ghost.
    - pre_scenario_review_screen.dart: GradientBackground root, GlassCard with scenario info (title, description, CefrBadge, difficulty). Tips section: GlassCard with bullet list. "Start Conversation" button: AppButton variant primary, large. "Back": AppButton variant ghost.

    After ALL changes, run `flutter analyze` on all modified files.
  </action>
  <verify>
    <automated>cd /Users/airm2/Desktop/linguo_wizard && flutter analyze lib/features/home/ lib/features/scenario_selection/ lib/features/srs/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "primaryPink\|bgTop\|bgBottom\|accentGold\|accentCoral\|textDark\|textMuted\|shadowPink\|AppColorProvider\|ThemeModeProvider\|primaryPinkDark\|primaryPinkLight" lib/features/home/screens/*.dart lib/features/home/widgets/*.dart lib/features/scenario_selection/screens/*.dart lib/features/scenario_selection/widgets/*.dart lib/features/srs/screens/*.dart` returns 0 for each file
    - home_screen.dart uses GradientBackground, has staggered card entrance animations with flutter_animate
    - All home widgets use GlassCard or glass-styled containers, semantic color tokens only
    - scenario_selection_screen.dart has GlassChip filter chips and GlassCard scenario grid
    - create_scenario_screen.dart has glass-filled form fields and AppButton variants
    - pre_scenario_review_screen.dart uses GlassCard for info and tips
    - `flutter analyze lib/features/home/ lib/features/scenario_selection/ lib/features/srs/` passes with zero errors
  </acceptance_criteria>
  <done>All 10 home and scenario screen/widget files restyled to futuristic dark theme. Zero legacy color references. Home dashboard has staggered card entrance animations, glass cards throughout, gradient backgrounds on all screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side rendering | All changes are visual screen/widget restyling, no data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | flutter_animate performance | low | accept | stagger with 50ms delay is lightweight; tune if needed |
| T-07-04 | Information Disclosure | None | n/a | n/a | No data flows in screen restyling |
</threat_model>

<verification>
- `flutter analyze lib/features/splash/ lib/features/onboarding/ lib/features/auth/ lib/features/home/ lib/features/scenario_selection/ lib/features/srs/` — zero errors
- `flutter test` — all 7 existing test files pass (D-07: zero business logic changes)
- Manual: open app, verify splash/onboarding/auth/home/scenario screens all render with dark theme, glass cards, gradient backgrounds, no pink/pastel remnants
</verification>

<success_criteria>
- All 18 screen/widget files use only new semantic color tokens (zero legacy references)
- All card containers use GlassCard component
- Home dashboard has staggered flutter_animate card entrance animations
- Scenario grid has staggered entrance animations
- Auth screens have glass form fields with accentCyan focus glow
- GradientBackground (MeshGradientBackground) on all screens
- flutter analyze passes on all modified feature directories
</success_criteria>

<output>
Create `.planning/phases/07-ui-redesign-dark-theme/07-02-SUMMARY.md` when done
</output>
