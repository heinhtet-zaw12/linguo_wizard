# Continue Here

## What Was Happening

Phase 2 (complete-mvp-features) is **100% complete**. The session hit context exhaustion while preparing to plan the next phase (Phase 3). The uncommitted changes are minor tweaks made after Phase 2 completion:
- Rate limiting toggled off (`rateLimitEnabled = false`) for development/testing
- Added `conversationProvider` invalidation on FeedbackScreen "Done" button to prevent stale state on re-entry
- STATE.md updated to reflect Phase 2 completion (40% overall progress)

## Files Modified But Not Committed

```
 M lib/core/config/app_config.dart          — Added rateLimitEnabled toggle (currently false)
 M lib/core/services/rate_limiter.dart      — Early-return when rateLimitEnabled is false
 M lib/features/feedback/screens/feedback_screen.dart — Invalidates conversationProvider on Done
 M .planning/STATE.md                       — Updated to show Phase 2 complete, 40%
?? .planning/phases/02-complete-mvp-features/02-02-SUMMARY.md — Plan 02 summary (new file)
```

## Exact Prompt to Resume

```
gsd-continue-work
```

## What Was Working On

Preparing to plan Phase 3 (Accounts & Cloud Sync). State file says "Ready to plan next phase."

## Key Context

- **Completed**: Phase 1 (foundation + core voice loop) and Phase 2 (complete MVP features — feedback, evaluation, rate limiting, onboarding persistence)
- **Next**: Phase 3 — Accounts & Cloud Sync (sign up/login, home dashboard, cloud sync)
- **Architecture**: MVVM + Feature-first. ViewModels are StateNotifiers. Services are injectable wrappers. Screens are pure UI.
- **Tech**: Flutter + Riverpod + Firebase + Gemini API
- **Theme**: 3D Claymorphism (soft, rounded, pastel)
- **Rate limiting**: Currently disabled for dev (`AppConfig.rateLimitEnabled = false`) — re-enable before shipping
- **Data model**: Guest-mode data structured to migrate cleanly to Firestore user doc on sign-up

## Key Files

| File | Purpose |
|------|---------|
| `.planning/STATE.md` | Master project state — update when resuming |
| `.planning/PROJECT.md` | Product requirements and roadmap |
| `.planning/phases/02-complete-mvp-features/` | Phase 2 plans and summaries |
| `lib/core/config/app_config.dart` | App-wide config constants |
| `lib/core/services/` | Service layer (STT, TTS, AI, RateLimiter, Evaluation) |
| `lib/features/conversation/` | Conversation feature (screen, viewmodel, providers) |
| `lib/features/feedback/` | Feedback feature (screen, viewmodel, score model) |
| `lib/features/onboarding/` | Onboarding feature |
| `lib/features/scenarios/` | Scenario selection feature |

## Don't Forget

- Before shipping: set `AppConfig.rateLimitEnabled = true`
- Phase 3 requires Firebase Auth setup (email, Google, continue-as-guest)
- Guest-mode data must migrate cleanly to Firestore — don't break the schema
- AI system prompts should stay in config layer, not hardcoded per-screen
